# LexOne Backlog (canonical, story-tracked)

Each story has an id, description, and status. This file is the source of
truth for status going forward — `APIBacklog.md` (technical endpoint specs)
and `MobileBacklog.MD` (mobile-specific detail) remain as reference detail
underneath these stories, not separate tracking.

Status values: `Done`, `In Progress`, `Todo`, `Blocked`.

| ID | Description | Status | Notes |
|---|---|---|---|
| STORY-001 | Clients backend: list/get clients, save lawyer-private notes (`node_api/models/clientModel.js`, `controllers/web/clientController.js`, `routes/clients.js`) | Done | See `APIBacklog.md` §1 |
| STORY-002 | Cases + Documents backend: list/get cases, list/upload versioned documents (`caseModel.js`, `caseController.js`, `routes/cases.js`, `middlewares/upload.js`) | Done | See `APIBacklog.md` §2-3. Local disk storage via multer, no S3 configured |
| STORY-003 | Calendar backend: weekly availability, manual blocks, public slot generation, appointment scheduling (online/cash payment), Stripe webhook, revenue aggregation | Done | See `APIBacklog.md` §4 |
| STORY-004 | Frontend: switch Clients/Cases/Calendar pages from `mockAuth.js` mock data to the real `node_api` endpoints | Todo | One page at a time, per `APIBacklog.md` "Frontend switch" notes |
| STORY-005 | `agent_api/` — standalone Python/FastAPI service, Gemini-backed, `/agent/chat` endpoint, replaces `simulateAiLawyerMatch` in `web_src/src/utils/aiLawyerMatch.js` | Todo | Not started. Decided: own service, not bolted into `node_api`, not MCP |
| STORY-006 | Mobile (Android/iOS): Clients module — list/detail screens | Todo | Mirrors STORY-001. See `MobileBacklog.MD` §1 |
| STORY-007 | Mobile (Android/iOS): Cases module — list/detail, document management, activity history | Todo | Mirrors STORY-002. See `MobileBacklog.MD` §2 |
| STORY-008 | Mobile (Android/iOS): Calendar module — agenda view for appointments | Todo | Mirrors STORY-003. See `MobileBacklog.MD` §3 |
| STORY-009 | Mobile (Android): Tasker Home Dashboard — tappable stat cards, "Mis Clientes" preview | In Progress | "Próximas citas" preview still pending. See `MobileBacklog.MD` §4 |
| STORY-010 | Mobile (Android): "Find Lawyers" AI-assisted search entry point + chat screen | In Progress | Entry point added; conversational screen is a placeholder pending STORY-005 |
| STORY-011 | Mobile (Android): Relocate "Delete Account" from main profile menu to bottom of Edit Profile settings | Done | See `MobileBacklog.MD` §6 |
| STORY-012 | Legacy PHP/Laravel admin panel (business-owner admin, lives at `LexOneWeb-API` root: `app/`, `routes/`, `vendor/`, etc.) — refactor/modernize | Todo | Confirmed still in active use (not dead code) — admin panel for the business owner. Scope/target stack not yet decided |
| STORY-013 | Split `lexone-monorepo` into separate repos: `lexone-web` (web_src), `lexone-api` (node_api), `lexone-ios`, `lexone-android` | In Progress | Fresh history (no secrets carried over) for web/api. Android already has its own repo (`TudoAndroidv2`) and remote; iOS has local history, no remote yet |
| STORY-014 | Deploy infra: AWS ECR + EC2 (`muuyal.tech`) + MongoDB Atlas + nginx/Certbot for `lex-one.online` | Done | `lexone-api` and `lexone-web` both deploy end-to-end (ECR build/push → EC2 pull/up). All 4 domains live with valid TLS: `lex-one.online`, `www.lex-one.online` (web, 200), `api.lex-one.online`, `ws.lex-one.online` (api/socket, 404 root is expected — no root route). See `INFRA.md` |
| STORY-015 | Security: MailerSend API keys are hardcoded in `node_api/src/controllers/mailController.js` (not env-based) | Todo | Move to `MAILERSEND_API_KEY` env var and rotate the exposed key |
| STORY-016 | Security: legacy `lexone-monorepo` repo has real secrets committed in tracked `.env` files (git history) | Todo | Explicitly deferred by the team — revisit separately, not blocking the repo split |
| STORY-017 | Bug: `web_src/package.json` had `homepage: "https://tudofyapp.com/web/"` left over from the old deployment, breaking all static asset loading (404→SPA fallback→`Unexpected token '<'`) once served from `lex-one.online` root | Done | Fixed by setting `homepage: "/"`, redeployed, verified `/static/js/...` and `/manifest.json` now return 200 |
| STORY-018 | Bug: `node_api/src/controllers/cronController.js` crashed every minute (`Cannot read properties of null (reading 'stripeChange')`) because the fresh `lexone_new` DB has no `Setting` document | Done | Added a null guard before `appSettings.stripeChange`; verified no more crash in logs after redeploy. Doesn't address the underlying gap of no seeded Setting doc — may want a seed script later |
| STORY-019 | Bug: homepage entered a full-page reload loop when `/web/api/v1/user/home` returned `status_code: 401`; unknown user IDs also caused the API to build invalid booking queries instead of behaving as anonymous requests | Done | Removed the homepage `window.location.reload(false)` call and render the anonymous state after clearing stale auth. `/user/home` now returns normal `status_code: 200` homepage content with empty recent items for anonymous or unknown users; disabled accounts still return `status_code: 401`. |
| STORY-020 | Bug: protected Calendar/Clients/Cases sub-routers were mounted at `/` before existing web routes, causing their root-level `commonJwt` middleware to return `401 Unauthorized access` for public endpoints including `/user/home` and `/appdefaults`; the homepage auth-reset rerender then repeatedly remounted `Home` | Done | Moved the protected feature routers after the existing public routes and made the frontend auth invalidation callback idempotent. Anonymous homepage and app-default requests can now reach their intended controllers without authentication. |
| STORY-021 | Bug: lawyer signup rendered a white page when `general_info` was not yet cached because `TaskerSignup` replaced its safe initial object with `null` and dereferenced `general_info.instant_location` during render | Done | Default missing cached app settings to `{}`, guard the location-mode lookup, and default a missing city list to an empty array so `/tasker/tasker-signup` renders while app defaults load. |
| STORY-022 | Bug: lawyer signup ("Office address") loading spinner could spin forever if browser geolocation failed/was denied (no error-path reset, no timeout); also no way to type an address manually if auto-detect didn't work | Done | `TaskerSignup.js`: reset `is_loading` in the geolocation error callback, added a `timeout`/`maximumAge` to `getCurrentPosition`, and added an "Enter address manually" link that unlocks the field and disables further auto-geolocation on focus. |
| STORY-023 | Bug: lawyer signup showed "Something Went Wrong!" because `/web/api/v1/appdefaults` crashed on every request (`Cannot read properties of null`) — the production `lexone_new` MongoDB had zero `Setting` documents, and `adminController.appDefaults` dereferences `appSettings.*` ~25 times with no null guard, so the catch block returned the generic error and the frontend got an empty `general_info: {}` | Done | Root cause was a missing seed, not a code bug: created one `Setting` document in production MongoDB with the required fields (`siteName`, `contactEmail`, `copyrightText`, `guideLine*`) filled in; schema defaults cover the rest. Also resolves the underlying gap noted in STORY-018. Verified `/web/api/v1/appdefaults` now returns real data instead of crashing. |
| STORY-024 | Despacho (legal office) accounts: PIN-based signup/signin, roster, per-lawyer signin, seat-capped `addlawyer` (lawyers added this way get `plan: "pro"` directly) (`node_api/src/controllers/web/despachoController.js`, `routes/WebmainRoutev1.js`) | Done | See `Endpoints.md` "Despacho (Legal Office) Accounts" |
| STORY-025 | Cases sharing: case creation, despacho-colleague listing, and `assign` endpoint to share a case with colleagues (`caseModel.js` `assigned_tasker_ids`, `caseController.js`) | Done | See `Endpoints.md` "Cases". Frontend: Case detail "Compartir con colegas" UI |
| STORY-026 | Admin panel (web-only): admin role + JWT auth, user list/filter, CSV export, freeze/unfreeze/delete accounts, manual plan override, pricing tab (`adminAuthController.js`, `adminController.js` listUsers/exportUsersCsv/freezeUser/unfreezeUser/deleteUser/setUserPlan/getPricing/updatePricing, `web_src/src/Pages/Admin/`) | Done | See `Endpoints.md` "Admin Panel". Admin accounts are seeded only (`node_api/scripts/seedAdmin.js`), no public signup. Portal: `/admin/admin-login` |
| STORY-027 | Tasker subscription plan model: trial (1 month) → basic/pro, storage limits (500MB/5GB) tracked via `file_size_bytes` on case documents, plan-selection UI (`userModel.js` plan/trialEndsAt, `planController.js`, `web_src/src/Pages/Tasker/AccountPlan.js`) | Done | See `Endpoints.md` "Tasker Subscription Plan". No real payment processor — plan state only, admin can override. Despacho-added lawyers get `plan: "pro"` directly (no trial) |
| STORY-028 | Mobile (Android/iOS): Despacho roster/lawyer-signin flow and Cases "share with colleagues" UI | Todo | Mirrors STORY-024/025. See `MobileBacklog.MD` §7 |
| STORY-029 | Mobile (Android/iOS): Tasker subscription plan screen — trial countdown, storage used/limit, basic/pro plan selection | Todo | Mirrors STORY-027. See `MobileBacklog.MD` §8. No admin panel equivalent needed on mobile (web-only, staff tool) |
| STORY-030 | API: search ranking by plan (Pro lawyers ranked above Basic in "Find Lawyers" results) | Todo | Deferred — `FindLawyers.js`/`agent_api/` (STORY-005) has no real search endpoint yet to sort. `plan` is already stored on the user, ready to wire in once that endpoint exists |
